/**
 * Migration drift check — Phase 1 stub.
 * Full implementation: supabase db diff + gen types per SAMELOBBY_TEST_IMPLEMENTATION_BUILD_PLAN.md §L.
 */
console.log("check-migration-drift: stub (Phase 1) — wire to supabase db diff in CI job");
